{
  "name": "worker-preview",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@workspace/shared": "workspace:*",
    "@workspace/worker-core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
